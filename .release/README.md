# 共享发布接入状态

入口：Actions → 发布软件。目标版本 `v0.1.74`，本仓库 `main`，三平台及 macOS Universal。

**当前是预检接入，尚未完成新版三平台构建。** 预检准确输出缺项并停止，不使用旧 v0.1.73 工作流替换新版验收门禁。

必须先解决：Windows 精确 `0.36.0-omb.2` vendor 来源；Mac Feishu 双架构与签名合同；源码/编译内容绑定的真实验收回执；Linux 品牌发布门禁。详见运行摘要和 `发布交付指南/`。

Enterprise 公开再分发授权已经由仓库所有者确认；不需要重复询问。应用内部标识、安装身份和服务数据目录应遵循新指南，不能通过批量改名破坏升级身份。

原 `private-release.yml` 是历史 v0.1.73 流程，继续保留供追溯。共用工具库在 https://github.com/rj-liukaiwen/release-kit ，通过固定 SHA 共享版本、产物与 Release 逻辑；本项目适配工作仍未完成。
